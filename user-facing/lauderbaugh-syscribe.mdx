---
title: "lauderbaugh/syscribe — USER_FACING Documentation"
description: "Auto-generated by Syscribe from PR #0"
---

\# Draft Review Editor: Action Bar & Busy-State Improvements ## What changed The draft review editor has been updated with several improvements that make it easier to take actions on documentation drafts — and harder to accidentally trigger multiple actions at once. \*\*Action bar placement is now flexible.\*\* The editor supports a fixed bottom action bar that stays visible as you scroll through longer drafts. When this mode is active, the editor content automatically adjusts its spacing so nothing is hidden behind the bar. \*\*Buttons disable themselves while an action is in progress.\*\* When you save, approve, or reject a draft, the relevant controls lock until the operation finishes. This prevents accidental double-submissions and makes it clear when the editor is busy. \*\*Inline draft actions appear contextually.\*\* Approve, reject, and save controls now show or hide based on the current state of the draft you're viewing — you won't see actions that aren't relevant to where a draft is in its workflow. \*\*Switching tabs no longer loses your place.\*\* The selected draft is now tracked more reliably when you switch between document tabs in a pull request review. The History tab continues to work as before. ## How to use it No changes to your workflow are required. Everything works through the same editor interface: 1. Open a pull request review as usual. 2. Select a document tab to view a draft. 3. Use the \*\*Save\*\*, \*\*Approve\*\*, or \*\*Reject\*\* buttons to act on the draft. If any of those actions is already running, the buttons will appear disabled — wait for the current action to complete before trying again. 4. If the action bar appears at the bottom of the screen, it stays fixed as you scroll, so you can always reach the controls without scrolling back to the top. ## What to expect - \*\*While saving, approving, or rejecting\*\*, action buttons are temporarily disabled. They re-enable automatically once the operation finishes. - \*\*On the History tab\*\*, draft-specific actions are hidden — they only appear when you have a specific document tab selected. - \*\*If a draft is marked Not Applicable\*\*, the editor will continue to show the appropriate panel rather than editable controls. - The visual spacing at the bottom of the editor may look slightly different depending on whether the fixed action bar is shown — this is intentional and ensures content isn't obscured.
